<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram Post Generator</title>
    <style>
        body { font-family: sans-serif; max-width: 600px; margin: 2rem auto; padding: 0 1rem; }
        input[type="text"] { width: 100%; padding: 0.5rem; margin-bottom: 1rem; }
        button { padding: 0.5rem 1rem; background-color: #007bff; color: white; border: none; cursor: pointer; }
        button:hover { background-color: #0056b3; }
        .error { color: red; margin-bottom: 1rem; }
    </style>
</head>
<body>
    <h1>Generate Instagram Post</h1>
    <% if (errorMessage) { %>
        <div class="error"><%= errorMessage %></div>
    <% } %>
    <form action="/generate" method="POST">
        <label for="topic">Enter Topic:</label>
        <input type="text" id="topic" name="topic" placeholder="E.g., Summer beach vibes with a dog" required>
        <button type="submit">Generate Post</button>
    </form>
    <p style="margin-top:1rem;">
        <a href="/classes">Go to Classes Management</a>
    </p>
</body>
</html>
